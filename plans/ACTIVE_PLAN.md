# No Active Plan

There is no active multi-step plan. For non-trivial work, ask `st-moritz-architect` to replace this file using `plans/TASK_TEMPLATE.md` without implementing production code.

Use `st-moritz-architect-deep` only after the default architect records a specific unresolved reasoning-limited design problem and supporting evidence.
